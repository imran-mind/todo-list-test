
export const config = {
    // loginUrl: 'http://localhost:5000/login',
    // todosUrl: 'http://localhost:5000/todos'
    loginUrl: `http://${window.location.hostname}:5000/api/login`,
    todosUrl: `http://${window.location.hostname}:5000/api/todos`
}