
export const config = {
    // loginUrl: 'http://localhost:5000/login',
    // todosUrl: 'http://localhost:5000/todos'
    loginUrl: `http://${location.hostname}:5000/login`,
    todosUrl: `http://${location.hostname}:5000/todos`
}